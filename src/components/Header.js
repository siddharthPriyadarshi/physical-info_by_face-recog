const Header = () => {
  return (
    <header
      className="header"
      style={{
        fontSize: '3rem',
        display: 'flex',
        justifyContent: 'center',
        fontWeight: '800',
        }}
    >
      Physical info through face recognition
    </header>
  )
}

export default Header
